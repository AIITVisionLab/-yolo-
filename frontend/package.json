{
  "name": "plant-frontend-shell",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5500",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5500 --strictPort",
    "test:e2e": "playwright test tests/e2e --reporter=line --workers=1 --timeout=60000"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-react": "^5.0.2",
    "vite": "^7.1.6"
  }
}
